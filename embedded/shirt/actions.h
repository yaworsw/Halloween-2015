/**
 * actions.h
 *
 * Includes all of headers for the possible actions the costume can do.  Also
 * defines the ActionType enum which is used to map serial event types to
 * subclasses of Action
 *
 */

#include "BlinkBean.h"

enum ActionType {
  BLINK_BEAN   = 0
};
